language: java
jdk:
  - openjdk8

branches:
  only:
    - master

# Travis CI 서버의 Home
cache:
  directories:
    - $HOME/.m2
    -
before_install:
  - chmod +x gradlew
  - 
# CI 실행 완료시 메일로 알람
notifications:
  email:
    recipients:
      - lsh503984@gmail.com