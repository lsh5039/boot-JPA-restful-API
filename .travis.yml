language: java
jdk:
  - openjdk8

branches:
  only:
    - master

# Travis CI 서버의 Home
cache:
  directories:
    - $HOME/.m2/repository
    - $HOME/.gradle
before_install:
  - chmod +x gradlew
#script: ".gradlew clean build" 얘가 안되네

# CI 실행 완료시 메일로 알람
notifications:
  email:
    recipients:
      - lsh503984@gmail.com